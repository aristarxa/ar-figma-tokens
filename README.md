# Token Manager - Плагин для управления токенами Figma

Плагин для просмотра, фильтрации и переноса токенов (variables) между коллекциями в Figma.

## Возможности

### 📋 Просмотр токенов
- Выбор коллекции переменных (по умолчанию первая коллекция)
- Иерархическое отображение переменных в виде дерева
- Группировка по структуре имен (например, `tag/bg/default` → группа `tag` → подгруппа `bg` → токен `default`)
- Схлопывание/раскрытие групп (по умолчанию все группы схлопнуты)
- Отображение типов переменных (COLOR, FLOAT, STRING, BOOLEAN)

### 🔄 Перенос токенов между коллекциями
- Множественный выбор токенов и групп через чекбоксы
- Кнопка "Swap Collection" для переноса выбранных токенов
- Выбор целевой коллекции из списка
- Автоматическое удаление из исходной коллекции после переноса
- Проверка на дубликаты с выводом ошибок

### 🔍 Поиск токенов
- Фильтрация по имени токена в реальном времени
- Автоматическое раскрытие групп при поиске
- Поиск по полному пути переменной

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/aristarxa/ar-figma-tokens.git
cd ar-figma-tokens
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Установка в Figma

1. Откройте Figma Desktop
2. Перейдите в меню **Plugins → Development → Import plugin from manifest...**
3. Выберите файл `manifest.json` из корня проекта
4. Плагин появится в меню **Plugins → Development → Token Manager**

**Примечание**: Плагин использует чистый JavaScript, сборка не требуется!

## Использование

### Просмотр токенов

1. Запустите плагин: **Plugins → Development → Token Manager**
2. Выберите коллекцию из выпадающего списка
3. Используйте треугольники (▶/▼) для раскрытия/схлопывания групп

### Поиск токенов

1. Введите текст в поле поиска
2. Плагин автоматически отфильтрует и раскроет найденные токены
3. Очистите поле для возврата к полному списку

### Перенос токенов

1. Отметьте чекбоксами нужные токены или целые группы
2. Нажмите кнопку **Swap Collection (N)** (где N - количество выбранных токенов)
3. Выберите целевую коллекцию из списка (введите номер)
4. Подтвердите перенос
5. Токены будут перенесены в целевую коллекцию и удалены из исходной

### Обработка ошибок

- Если токен с таким именем уже существует в целевой коллекции, будет выведено сообщение об ошибке
- Токен останется в исходной коллекции
- Другие токены из выборки будут перенесены успешно

## Структура проекта

```
src/
├── code.js              # Основной код плагина (чистый JavaScript)
└── ui.html              # Пользовательский интерфейс
```

## Разработка

### Изменение кода

1. Редактируйте `src/code.js` или `src/ui.html`
2. В Figma: **Plugins → Development → Token Manager** (перезапустите плагин)
3. Изменения применятся автоматически

### Отладка

1. В Figma откройте **Plugins → Development → Token Manager**
2. Откройте консоль разработчика:
   - **macOS**: `Cmd + Option + I`
   - **Windows**: `Ctrl + Shift + I`
3. Ошибки плагина будут отображаться в консоли с префиксом `[Plugin Error]`

## Технические детали

### Почему JavaScript вместо TypeScript?

Figma Plugin API имеет ограничения при работе с модулями ES6 и CommonJS. Использование чистого JavaScript:
- ✅ Избегает проблем с `exports is not defined`
- ✅ Не требует сборки
- ✅ Быстрее в разработке
- ✅ Проще для отладки

### Архитектура

- **code.js**: Монолитная структура со всей логикой в одном файле
- **Секции**: Типы → Утилиты → Сервисы → Обработчики → Инициализация
- **Коммуникация**: Обмен сообщениями между UI и плагином через `postMessage`

### Ограничения API

- При переносе переменной между коллекциями API Figma не позволяет изменить коллекцию напрямую
- Плагин создает новую переменную в целевой коллекции и удаляет старую
- Связи (bindings) переменной с элементами дизайна могут быть потеряны

## Технологии

- **JavaScript (ES2017)** - основной язык разработки
- **Figma Plugin API** - взаимодействие с Figma
- **Vanilla JS** - интерфейс без фреймворков для минимального размера

## Лицензия

MIT

## Автор

[aristarxa](https://github.com/aristarxa)

## Ссылки

- [Репозиторий](https://github.com/aristarxa/ar-figma-tokens)
- [Figma Plugin API Documentation](https://www.figma.com/plugin-docs/)
- [Figma Variables API](https://www.figma.com/plugin-docs/api/properties/figma-variables/)
