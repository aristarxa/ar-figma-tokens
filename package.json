{
  "name": "ar-figma-tokens",
  "version": "1.0.0",
  "description": "Figma plugin for managing design tokens/variables",
  "scripts": {
    "build": "echo 'No build needed - using plain JavaScript'",
    "dev": "echo 'Plugin ready for development'"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0"
  }
}
